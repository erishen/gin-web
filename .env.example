# Docker Compose 部署配置

# 数据库配置（生产环境使用 MySQL）
DB_ROOT_PASSWORD=rootpassword
DB_NAME=gin_web_db
DB_USER=ginuser
DB_PASSWORD=ginpassword

# 应用配置
PORT=3000
GIN_MODE=release

# 如果使用 SQLite（开发环境），请在 main.go 中修改 DATABASE_URL
# DATABASE_URL=sqlite:gin_web.db
